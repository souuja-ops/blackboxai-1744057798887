# Previous settings remain the same
ORS_API_KEY = 'your-actual-api-key-here'  # Replace with real API key

# OpenRouteService Configuration
ORS_API_KEY = os.getenv('ORS_API_KEY', 'your-actual-api-key-here')  # Set via environment variable

# Security Settings
ALLOWED_HOSTS = ['localhost', '127.0.0.1']
